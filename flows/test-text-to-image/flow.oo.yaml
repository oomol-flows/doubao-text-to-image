title: "%test-text-to-image%"

nodes:
  - node_id: text-to-image#1
    task: self::text-to-image
    inputs_from:
      - handle: prompt
        value: "a cute cat lying on a bed, looking at the camera"
  - node_id: +python#1
    title: "Python #1"
    icon: ":logos:python:"
    task:
      ui:
        default_width: 450
      inputs_def:
        []
      outputs_def:
        []
      executor:
        name: python
        options:
          entry: scriptlets/+scriptlet#1.py
