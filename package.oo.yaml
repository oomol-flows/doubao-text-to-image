name: doubao_text_to_image
displayName: "%display-name%"
description: "%description%"
version: 0.0.3
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
icon: ./icon.png
repository: https://github.com/oomol-flows/doubao-text-to-image.git
releaseNotes: init
